import { TxCall, KintoAccountInfo } from './types';
declare class KintoSDK {
    private kintoUrl;
    private appAddress;
    private iframeId;
    private modalId;
    constructor(appAddress: string);
    connect(): Promise<KintoAccountInfo>;
    sendTransaction(txs: TxCall[]): Promise<void>;
    createNewWallet(): Promise<void>;
}
export declare const createKintoSDK: (appAddress: string) => KintoSDK;
export {};
